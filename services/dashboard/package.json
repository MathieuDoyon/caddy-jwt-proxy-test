{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "Test docker proxy jwt",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production && node server.js",
    "dev": "NODE_ENV=development && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^7.1.9"
  }
}
